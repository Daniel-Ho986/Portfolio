<ul class="grid">
	<slot />
</ul>

<style>
	.grid {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		list-style: none;
		padding: 0;
	}

	.grid > :global(:nth-child(odd)),
	.grid > :global(:nth-child(even)) {
		align-self: center;
		width: 100%
	}

	@media (min-width: 55em) {
		.grid > :global(:nth-child(odd)) {
			align-self:  flex-start;
			width: 60%
		}

		.grid > :global(:nth-child(even)) {
			align-self:  flex-end;
			width: 60%
		}
	} 
</style>
